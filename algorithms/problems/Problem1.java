package problems;

/**
 * @author Fcb
 * @date 2020/2/23
 * @description
 * 给定一个数组，求如果排序之后，相邻两数的最大差值，要求时间复杂度O(n)，且不能用非基于比较的排序
 * 思路：假设数组长度是n，将数组分成n+1份，然后遍历。存boolean,max,min，不断用min-max，最后找到最大的
 */
public class Problem1 {

    public int maxGap(int[] arr){
        return 0;
    }
}
